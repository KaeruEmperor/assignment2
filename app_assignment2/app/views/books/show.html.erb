<div class = "notice">
    <%= flash[:notice] %>
</div>
<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<h4>Title:</h4><p><%= @book.title %></p>
<br>
<h4>Body</h4><p><%= @book.body %></p>
<br>
<%= link_to "Edit", edit_book_path(params[:id]) %>
<%= link_to "Back", "/books" %>